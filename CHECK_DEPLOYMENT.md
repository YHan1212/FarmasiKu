# 如何检查 Vercel 部署状态

## 方法 1: 通过 Vercel Dashboard（推荐）

1. **访问 Vercel Dashboard**
   - 打开：https://vercel.com/dashboard
   - 使用 GitHub 账号登录（如果项目已连接）

2. **查找你的项目**
   - 项目名称：`FarmasiKu` 或 `farmasiku`
   - 项目 URL：https://farmasiku.vercel.app

3. **查看部署历史**
   - 点击项目进入详情页
   - 查看 "Deployments" 标签
   - 检查最新的部署状态：
     - ✅ **Ready** = 部署成功
     - ⏳ **Building** = 正在构建
     - ❌ **Error** = 部署失败
     - 🔄 **Queued** = 等待部署

4. **检查部署时间**
   - 最新提交：`f1486e9` - "Add simple copy-paste SQL script for Supabase"
   - 如果最新部署时间早于这个提交，说明需要重新部署

## 方法 2: 通过 GitHub Actions（如果配置了）

1. 访问：https://github.com/YHan1212/FarmasiKu/actions
2. 查看是否有自动部署的 workflow

## 方法 3: 手动触发重新部署

### 选项 A: 通过 Vercel Dashboard
1. 登录 Vercel Dashboard
2. 进入项目页面
3. 点击 "Deployments" 标签
4. 点击 "Redeploy" 按钮（在最新部署旁边）

### 选项 B: 通过 Git Push（如果已配置自动部署）
```bash
# 创建一个空提交来触发部署
git commit --allow-empty -m "Trigger Vercel deployment"
git push origin main
```

## 方法 4: 检查生产环境代码版本

访问生产环境并检查：
1. 打开：https://farmasiku.vercel.app
2. 打开浏览器开发者工具（F12）
3. 查看 Console 日志，应该看到：
   ```
   [PharmacistDashboard] Loading waiting queues...
   [PharmacistDashboard] ✅ SUCCESS: Found X waiting queue(s)
   ```
4. 如果看到旧的日志格式，说明代码还未更新

## 方法 5: 检查构建文件时间戳

在浏览器中：
1. 打开开发者工具（F12）
2. 进入 Network 标签
3. 刷新页面（Ctrl+Shift+R 强制刷新）
4. 查看 `index-*.js` 文件的响应头
5. 检查 `Last-Modified` 或 `Date` 头，确认是否为最新构建

## 当前状态检查

**最新提交信息：**
- Commit: `f1486e9`
- 时间: 最近
- 消息: "Add simple copy-paste SQL script for Supabase"

**如果部署未更新，请：**
1. 等待 2-5 分钟（Vercel 自动部署通常很快）
2. 或手动触发重新部署
3. 清除浏览器缓存（Ctrl+Shift+R）

## 快速验证部署是否成功

运行以下命令检查生产环境的 JavaScript 文件：
```bash
# 检查生产环境文件（需要 curl 或浏览器）
# 访问：https://farmasiku.vercel.app/assets/index-*.js
# 搜索：".eq('status', 'waiting')"
# 如果找到，说明新代码已部署
```

