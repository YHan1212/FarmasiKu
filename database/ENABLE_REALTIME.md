# 启用 Supabase Realtime 功能

## 📋 说明

为了让管理面板能够实时显示新订单，需要启用 Supabase Realtime 功能。

---

## 🔧 设置步骤

### 步骤 1: 在 Supabase Dashboard 中启用 Realtime

1. 登录 Supabase Dashboard
2. 选择你的项目
3. 在左侧菜单中，点击 **"Database"**
4. 点击 **"Replication"** 标签
5. 找到 `orders` 表
6. 点击开关，启用 Realtime 复制

### 步骤 2: 验证设置

在 Supabase SQL Editor 中运行：

```sql
-- 检查 Realtime 是否已启用
SELECT 
  schemaname,
  tablename,
  replica_identity
FROM pg_tables
WHERE tablename = 'orders';
```

---

## ✅ 功能说明

启用后，管理面板将：

1. **自动更新**：当有新订单创建时，Orders 标签页会自动刷新
2. **实时统计**：Dashboard 的统计数据也会自动更新
3. **手动刷新**：如果实时更新不工作，可以点击 "🔄 Refresh" 按钮手动刷新

---

## 🔍 故障排除

### 如果实时更新不工作：

1. **检查 Realtime 是否已启用**
   - 在 Supabase Dashboard → Database → Replication 中确认

2. **检查浏览器控制台**
   - 按 F12 打开开发者工具
   - 查看 Console 是否有错误信息
   - 应该能看到 "Order change detected:" 日志

3. **使用手动刷新**
   - 点击 Orders 标签页中的 "🔄 Refresh" 按钮

4. **检查网络连接**
   - 确保网络连接正常
   - Supabase Realtime 需要 WebSocket 连接

---

## 📝 注意事项

- Realtime 功能需要 Supabase 项目处于活动状态
- 免费账户也支持 Realtime，但有限制
- 如果 Realtime 不可用，可以随时使用手动刷新按钮

---

完成！现在管理面板应该能够实时显示新订单了！🎉

