# Vercel 构建错误修复指南

## 🔍 错误：Cannot use 'import.meta' outside a module

这个错误通常发生在 Vercel 构建时，表示代码没有正确作为 ES 模块处理。

---

## ✅ 解决方案

### 方法 1: 更新 Vite 配置（已自动完成）

已更新 `vite.config.js` 以确保正确的模块格式：

```javascript
build: {
  target: 'esnext',
  modulePreload: {
    polyfill: true
  },
  rollupOptions: {
    output: {
      format: 'es'
    }
  }
}
```

### 方法 2: 检查 Vercel 构建设置

1. 在 Vercel Dashboard 中，进入项目设置
2. 点击 **Settings** → **General**
3. 确认以下设置：
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`

### 方法 3: 清除缓存并重新部署

1. 在 Vercel Dashboard → Deployments
2. 找到最新的部署
3. 点击 **"..."** → **Redeploy**
4. 勾选 **"Use existing Build Cache"** 的**反选**（清除缓存）
5. 点击 **Redeploy**

---

## 🧪 本地测试构建

在推送到 Vercel 之前，可以在本地测试构建：

```bash
# 安装依赖
npm install

# 构建项目
npm run build

# 预览构建结果
npm run preview
```

如果本地构建成功，Vercel 构建也应该成功。

---

## 🔍 常见问题

### 问题 1: 构建失败，显示模块错误

**原因**: Node.js 版本不兼容

**解决**: 在 Vercel 项目设置中，确保 Node.js 版本为 18+：
- Settings → General → Node.js Version → 选择 18.x 或 20.x

### 问题 2: 构建成功但运行时错误

**原因**: 环境变量未正确注入

**解决**: 
1. 确认环境变量已在 Vercel 中设置
2. 重新部署应用

### 问题 3: 本地正常，Vercel 失败

**原因**: 构建配置差异

**解决**: 
1. 检查 `package.json` 中的构建脚本
2. 确认 `vite.config.js` 配置正确
3. 查看 Vercel 构建日志

---

## 📋 检查清单

- [ ] `vite.config.js` 已更新（包含 `build` 配置）
- [ ] `package.json` 中的构建脚本正确
- [ ] Vercel 项目设置中 Framework Preset 为 Vite
- [ ] Node.js 版本为 18+（在 Vercel 设置中）
- [ ] 已清除构建缓存并重新部署
- [ ] 本地构建测试成功

---

## 🆘 如果仍然失败

1. **查看构建日志**：
   - Vercel Dashboard → Deployments → 点击失败的部署
   - 查看 Build Logs 中的详细错误信息

2. **检查依赖**：
   - 确认 `package.json` 中所有依赖都是最新版本
   - 运行 `npm update` 更新依赖

3. **联系支持**：
   - Vercel 支持: https://vercel.com/support
   - Vite 文档: https://vitejs.dev/guide/build.html

---

**完成以上步骤后，构建应该可以成功了！** ✅

